import { DEFAULT_PAGE_SIZE_OPTIONS, paginateItems } from '../../../components/Common/pagination';

export const TASK_LIST_PAGE_SIZE_OPTIONS = DEFAULT_PAGE_SIZE_OPTIONS;
export const paginateTaskListItems = paginateItems;
