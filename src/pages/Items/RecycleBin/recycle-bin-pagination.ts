import { DEFAULT_PAGE_SIZE_OPTIONS, paginateItems } from '../../../components/Common/pagination';

export const RECYCLE_BIN_PAGE_SIZE_OPTIONS = DEFAULT_PAGE_SIZE_OPTIONS;
export const paginateRecycleBinItems = paginateItems;
